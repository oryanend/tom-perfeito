# DATASOURCE
spring.datasource.url=jdbc:postgresql://${PG_HOST_PROD}:${PG_PORT_PROD}/${PG_DATABASE_PROD}
spring.datasource.username=${PG_USER_PROD}
spring.datasource.password=${PG_PASSWORD_PROD}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false


# FLYWAY
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=false

# Hikari Configs (Render)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.data-source-properties.cachePrepStmts=true

# CORS - Frontend allowed origins
# Use environment variable APP_CORS_ALLOWED_ORIGINS to override in production (comma-separated list)
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:4200}

# Bcrypt strength
security.bcrypt.strength=8

# Redis Config
spring.data.redis.ssl.enabled=true
spring.data.redis.url=${REDIS_URL}
spring.cache.type=redis