{
  "name": "tom-perfeito",
  "version": "1.0.0",
  "description": "<h1 align=\"center\">Tom Perfeito</h1>",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky",
    "commit": "cz",
    "jacoco": "mvn clean test jacoco:report",
    "sonar:start": "mvn clean verify sonar:sonar -Dsonar.host.url=http://localhost:9000 -Dsonar.token=sqa_64b59052bdc8d022bd1be1cabb1439f58284492c",
    "sonar:logs": "docker compose logs -f sonarqube",
    "services:up": "docker-compose -f docker-compose.yml up -d -v",
    "service:stop": "docker-compose -f docker-compose.yml stop",
    "services:down": "docker-compose -f docker-compose.yml down -v"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^20.3.1",
    "@commitlint/config-conventional": "^20.3.1"
  },
  "dependencies": {
    "commitizen": "^4.3.1",
    "husky": "^9.1.7"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
